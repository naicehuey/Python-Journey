# Listdir and Join — First OS Practice

The very first test of the `os` module.
Just checking if listing files and building paths actually works.

## What I Was Practicing
- `os.listdir()` — listing all files in a folder
- `os.path.join()` — combining folder path and filename

## Note
This is where `os` module learning began —
one loop, one print, just seeing if it works.
Everything in `00_syntax_practice/` grew from here.